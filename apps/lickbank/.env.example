# LickBank environment variables

# SQLite database location. LickBank uses Prisma against a single .db file.
# Path is relative to apps/lickbank/, so the default points at the monorepo's
# data/ directory (kept out of git) for a clean dev experience.
DATABASE_URL="file:../../data/dev.db"
